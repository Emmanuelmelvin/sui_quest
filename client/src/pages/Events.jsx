import {
  ConnectButton,
  useWallet,
  addressEllipsis,
} from "@suiet/wallet-kit";
import '../styles/event.css';


const Events = () => {
  const wallet = useWallet();

  return (
    <div>
    Hello
    </div>
  );
};

export default Events;
